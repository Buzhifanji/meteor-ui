/* eslint-disable no-restricted-globals */
import { defineCustomElement } from "utils";
import { Selector, SelectorAll } from "utils/dom";
import { getStylePropertyValue } from "utils";
import { V{{properCase name}} } from '../src'

defineCustomElement("v-{{name}}", V{{properCase name}});

describe("web component v-{{name}}", () => {
    beforeEach(() => {
    document.body.innerHTML = `
        <v-{{name}} class="v-{{name}}"></v-{{name}}>
    `
    })
})