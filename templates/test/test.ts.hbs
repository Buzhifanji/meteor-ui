import '@testing-library/jest-dom';
import { render } from '@testing-library/svelte';
import {{properCase name}} from '../src/{{name}}.svelte';

describe('{{properCase name}} web component', () => {
  test('create', () => {
    const { container } = render({{properCase name}});
    expect(container).toContainHTML('{{name}}');
  });
});
